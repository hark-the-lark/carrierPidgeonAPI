# docker-compose.yml
version: "3.9"
services:
  carrierpidgeonapi:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - /home/eseantatum/data/carrierpidgeoncorpus:/app/corpus 
      - ./service:/app/service
    environment:
      - PYTHONUNBUFFERED=1
      - PYTHONPATH=/app